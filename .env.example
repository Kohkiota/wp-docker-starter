# ================================================
# WordPress Docker環境 設定ファイル
# ================================================
# 使用方法:
#   cp .env.example .env
#   vi .env  # 環境に応じて編集
# ================================================

# ================================================
# WordPress バージョン
# ================================================
WP_VERSION=6.8.2

# ================================================
# WordPress サイトURL設定
# ================================================
# ローカル開発:
WP_HOME=http://localhost:8080
WP_SITEURL=http://localhost:8080

# 本番環境: 以下に変更
# WP_HOME=https://example.com
# WP_SITEURL=https://example.com

# ================================================
# WordPress 管理者設定
# ================================================
WP_ADMIN_USER=admin
WP_ADMIN_PASSWORD=admin_password
WP_ADMIN_EMAIL=admin@example.com
# WPの通知用メールアドレス

# 本番環境: 強固なパスワードに変更すること
# WP_ADMIN_PASSWORD=ランダムな強固なパスワード
# WP_ADMIN_EMAIL=info@example.com

# ================================================
# データベース設定
# ================================================
WORDPRESS_DB_HOST=mysql:3306
WORDPRESS_DB_NAME=wordpress
WORDPRESS_DB_USER=wpuser
WORDPRESS_DB_PASSWORD=wppassword

# 本番環境: データベース名とパスワードを変更すること
# WORDPRESS_DB_NAME=wordpress_prod
# WORDPRESS_DB_USER=wpuser_prod
# WORDPRESS_DB_PASSWORD=ランダムな強固なパスワード

# データベーステーブルプレフィックス（セキュリティ向上）
# デフォルト: wp_  推奨: ランダムな3-5文字 + アンダースコア
WORDPRESS_TABLE_PREFIX=wp_
# 本番環境: プレフィックスは変更すること
# WORDPRESS_TABLE_PREFIX=k9mx_

# ================================================
# MySQL Root設定
# ================================================
MYSQL_ROOT_PASSWORD=rootpass

# 本番環境: 必ず強固なパスワードに変更すること
# MYSQL_ROOT_PASSWORD=ランダムな強固なパスワード

# ================================================
# 注意事項
# ================================================
# 1. このファイルは .gitignore に含めること
# 2. 本番環境では全てのパスワードを変更すること
# 3. パスワード生成例: openssl rand -base64 32

# ================================================
# Google Drive 自動バックアップ設定
# ================================================
# セットアップ: sudo bash scripts/setup-gdrive.sh
#   → 全自動 → URLクリック → 認証 → コード貼り付け → 完了
# backup.sh実行時にGoogle Driveへ自動アップロード
# GDRIVE_REMOTE_NAMEなし → Google Driveに保存しない
GDRIVE_REMOTE_NAME="gdrive"

# Google Drive上の保存先フォルダ
# 例: wordpress-backups/mysite → マイドライブ/wordpress-backups/mysite/
# 空 + GDRIVE_REMOTE_NAMEあり → マイドライブ/backups/（デフォルト:推奨）
GDRIVE_FOLDER=""

# テーマ名設定（案件ごとに変更）
# wordpress/wp-content/themes/onlineprofiletheme ＜このディレクトリ名と一致させる。半角英数とハイフンのみ、スペース禁止。
THEME_NAME=onlineprofiletheme
# CF7が初期設定してくるフォームのタイトルをここと、wordpress/wp-content/themes/onlineprofiletheme/page-contact.php、のフォーム埋込みショートコードのtitle=""に正しく設定すること。
CF7_TITLE="Contact form 1"
